<div class="shop-list">
  <p><%= @shops.length%>件ヒットしました。</p>
  <% if @shops.present? %>
    <% @shops.each do |shop| %>
      <%= link_to shop do %>
        <p><%= shop.name %></p>
        <p><%= shop.address %></p>
      <% end %>
    <% end %>
  <% else %>
    <p>検索結果は見つかりませんでした。</p>
  <% end %>
</div>

<%= paginate @shops %>
